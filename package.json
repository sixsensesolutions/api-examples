{
  "name": "six-sense-api",
  "version": "1.0.0",
  "description": "Shift-left credential security API",
  "main": "src/handlers/generate.js",
  "scripts": {
    "test": "jest --coverage",
    "test:unit": "jest tests/unit",
    "test:integration": "jest tests/integration"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-ses": "^3.1028.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
